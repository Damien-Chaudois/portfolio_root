# Redirige tout le trafic HTTP vers HTTPS.
# Aucune exception — on ne sert jamais de contenu en clair.
server {
    listen 80;
    server_name _;  # Capture tous les domaines

    location / {
        return 301 https://$host$request_uri;
    }
}